# 📊 บันทึกผลการทดลอง - Workshop 9 Level 1

## ผู้ทดลอง
- ชื่อ: [ระบุชื่อ]
- วันที่: [ระบุวันที่]

## การทดลองที่ 1: ทดสอบคำสั่งพื้นฐาน

### คำสั่งที่ใช้:
```bash
node index.js create test1.txt "Hello Node.js"
node index.js list
node index.js read test1.txt
```

### ผลลัพธ์:
[บันทึกผลลัพธ์]

### สังเกต:
[บันทึกสิ่งที่สังเกตเห็น]

---

## การทดลองที่ 2: ทดสอบ Error Handling

### คำสั่งที่ใช้:
```bash
node index.js read nonexistent.txt
```

### ผลลัพธ์:
[บันทึกผลลัพธ์]

### สังเกต:
[บันทึกว่า error handling ทำงานอย่างไร]

---

## การทดลองที่ 3: ทดสอบการคัดลอกไฟล์

### คำสั่งที่ใช้:
```bash
node index.js create original.txt "Original content"
node index.js copy original.txt copied.txt
node index.js read copied.txt
```

### ผลลัพถ์:
[บันทึกผลลัพธ์]

### สังเกต:
[บันทึกสิ่งที่สังเกตเห็น]

---

## การทดลองที่ 4: ทดสอบการสร้างโฟลเดอร์

### คำสั่งที่ใช้:
```bash
node index.js mkdir test-folder
node index.js list
```

### ผลลัพธ์:
[บันทึกผลลัพธ์]

### สังเกต:
[บันทึกสิ่งที่สังเกตเห็น]

---

## การทดลองที่ 5: ทดสอบการลบไฟล์

### คำสั่งที่ใช้:
```bash
node index.js delete test1.txt
node index.js list
```

### ผลลัพธ์:
[บันทึกผลลัพธ์]

### สังเกต:
[บันทึกสิ่งที่สังเกตเห็น]

---

## สรุป

### สิ่งที่ได้เรียนรู้:
- [ ] Node.js modules และ require
- [ ] File System operations (async/await)
- [ ] Environment Variables (.env)
- [ ] Command Line Arguments (process.argv)
- [ ] Error Handling
- [ ] Logger สำหรับแสดงผลและบันทึก log

### ปัญหาที่พบ:
[บันทึกปัญหาที่พบระหว่างทดลอง]

### แนวทางแก้ไข:
[บันทึกวิธีแก้ไขปัญหา]

### ข้อเสนอแนะ:
[บันทึกข้อเสนอแนะสำหรับการพัฒนาต่อ]

---

## Challenge (ถ้ามีเวลา)

### Challenge 1: เพิ่มคำสั่ง append
```bash
# เพิ่มข้อความต่อท้ายไฟล์
node index.js append sample.txt "New line"
```

**สถานะ:** [ ] ยังไม่ทำ / [ ] กำลังทำ / [ ] เสร็จแล้ว

**บันทึก:**
[บันทึกวิธีการทำและผลลัพธ์]

---

### Challenge 2: เพิ่มคำสั่ง search
```bash
# ค้นหาไฟล์ที่มีข้อความที่ต้องการ
node index.js search "keyword"
```

**สถานะ:** [ ] ยังไม่ทำ / [ ] กำลังทำ / [ ] เสร็จแล้ว

**บันทึก:**
[บันทึกวิธีการทำและผลลัพธ์]

---

### Challenge 3: เพิ่มคำสั่ง stats
```bash
# แสดงข้อมูลรายละเอียดของไฟล์
node index.js stats sample.txt
```

**สถานะ:** [ ] ยังไม่ทำ / [ ] กำลังทำ / [ ] เสร็จแล้ว

**บันทึก:**
[บันทึกวิธีการทำและผลลัพธ์]

---

## คะแนนประเมินตนเอง
- ความเข้าใจ Node.js modules: [ ] / 10
- ความเข้าใจ File System operations: [ ] / 10
- ความเข้าใจ Environment Variables: [ ] / 10
- ความเข้าใจ Command Line Arguments: [ ] / 10
- ความเข้าใจ Async/Await: [ ] / 10

**คะแนนรวม:** [ ] / 50
